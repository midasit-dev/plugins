// VBA: main.bas > 재료 이름 변환 (긴 이름 → MCT용 축약)
// TODO: 구현 필요

/**
 * ES 재료 이름을 MCT용 이름으로 변환
 * VBA: m_dicMatlName Dictionary 참조
 * @param _esName ES 원본 재료 이름
 * @returns MCT용 변환된 이름
 */
export function changeMatlName(_esName: string): string {
  // TODO: VBA main.bas의 재료 이름 변환 로직 구현
  return '';
}
