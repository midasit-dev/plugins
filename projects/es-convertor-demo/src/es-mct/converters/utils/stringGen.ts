// VBA: main.bas > GetStringGen() — 노드/요소 번호 범위 문자열 생성
// 입력: "1,2,3,4,5,10,11,12" → 출력: "'1to5 10to12"
// TODO: 구현 필요

/**
 * 번호 배열을 MCT 범위 문자열로 변환
 * VBA: GetStringGen() (main.bas)
 * @param _numbers 정렬된 번호 배열
 * @returns MCT 범위 표기 문자열
 */
export function getStringGen(_numbers: number[]): string {
  // TODO: VBA main.bas의 GetStringGen() 로직 구현
  return '';
}
